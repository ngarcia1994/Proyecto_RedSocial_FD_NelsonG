import { people } from `../components/user/People`;

export const Routing = {} => {
  return (
    <BrowserRouter>
      <Router>
        {/* Cargamos los componentes de la ruta publica */}
        <Router path='/' element={ <Publiclayout />}>
          <Route index elemen
        
        
        
        
        </Router>







      </Router>
    </BrowserRouter>
  )







}